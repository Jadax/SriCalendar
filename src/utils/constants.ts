/** Number of milliseconds before a local record is synchronized. */
export const SYNC_DEBOUNCE_MS = 500;
/** Number of cloud retry attempts before entering offline mode. */
export const SYNC_RETRIES = 3;
/** Product display name. */
export const APP_NAME = 'SriCalendar';
